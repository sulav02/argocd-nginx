apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-declarative-approach-app
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/sulav02/argocd-nginx
    targetRevision: main
    path: argo-declarative

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true